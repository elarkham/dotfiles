#!/bin/env bash
##Ethan Larkham
##Mar 01 2015

if [[ $(ip addr show dev "$(getWired)" | grep "inet ") != "" ]];
    then echo "$(getWired)"
elif [[ $(ip addr show dev "$(getWifi)" | grep "inet ") != "" ]];
    then echo "$(getWifi)"
else
    echo "Unkown"
fi
